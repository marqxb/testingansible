---
- hosts: all
  tasks:
  - name: update using dnf
    become: true 
    ansible.builtin.dnf:
      name: "*"
      state: latest
    when: ansible_pkg_mgr == 'dnf'

  - name: update using apt
    ansible.builtin.apt:
      name: "*"
      state: latest
    when: ansible_pkg_mgr == 'apt'
...
